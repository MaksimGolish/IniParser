package model.points;

import model.storage.AbstractStorage;

public class FullRestorePoint extends RestorePoint {
    public FullRestorePoint(AbstractStorage storage) {
        super(storage);
    }
}
